#!/usr/bin/env python3

# Define the objects x, y, and z with their respective values
x = 10
y = 2
z = 5

# Perform the arithmetic operations and store the result in the result variable
result = x + y * z

# Print out the formatted string showing the arithmetic expression and its result
print(str(x) + " + " + str(y) + " * " + str(z) + " = " + str(result))
